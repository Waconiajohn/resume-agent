/**
 * @deprecated Import from './resume/types.js' directly.
 *
 * This barrel re-exports everything from the canonical location at
 * `agents/resume/types.ts`. It exists purely for backward compatibility
 * while import paths are migrated. All new code should import from
 * `agents/resume/types.js`.
 */
export * from './resume/types.js';
