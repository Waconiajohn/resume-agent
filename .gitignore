node_modules/
dist/
*.tsbuildinfo

# Environment secrets
.env*
!.env.example
!app/.env.example
!server/.env.example
app/.env*
server/.env*
*.log
.DS_Store
*.png
*.jpg
*.jpeg
.mcp.json
supabase/.temp/
.auth/
playwright-report/
test-results/
.mcp.json.backup
.mcp.json.broken.*
.playwright-mcp/
app/docs/agentic-ai-planning/
docs/obsidian/.obsidian/

# v3 resume fixtures — raw resumes and metadata may contain PII.
# The README remains tracked; snapshots are regenerated on demand.
server/test-fixtures/resumes/raw/
server/test-fixtures/resumes/meta/
server/test-fixtures/resumes/extracted/
server/test-fixtures/resumes/fixture-map.json
server/test-fixtures/snapshots/

# v3 job description fixtures — raw JDs are public postings but treated
# the same way as resumes for consistency with the resume corpus.
# The README remains tracked.
server/test-fixtures/job-descriptions/raw/
server/test-fixtures/job-descriptions/meta/
server/test-fixtures/job-descriptions/extracted/

# Cover-letter comparison harness — per-run outputs are regenerated on
# demand and may contain PII from real fixtures. The fixtures directory
# and README remain tracked.
server/test-fixtures/cover-letters/results/
