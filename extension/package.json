{
  "name": "careeriq-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "BUILD_ENTRY=background vite build --watch",
    "build": "tsc --noEmit && BUILD_ENTRY=background vite build && BUILD_ENTRY=content vite build && BUILD_ENTRY=popup vite build && node scripts/copy-static.js",
    "build:zip": "npm run build && cd dist && zip -r ../careeriq-extension.zip .",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.3",
    "vite": "^6.2.4",
    "vitest": "^3.0.8"
  }
}
